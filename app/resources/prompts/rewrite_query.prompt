历史会话：
{history_text}

当前用户问题：{question}

请只输出一个 JSON 对象（不要输出任何其他内容）：
1. rewritten_query：把当前问题改写为独立完整、包含知识领域关键词的问句，不超过 100 字；
2. domains：从当前问题与历史会话中识别知识领域（如：绿建、双碳、建筑防火、无障碍、碳交易），最多 3 个；无法确定时返回空列表。

格式：
{{"rewritten_query": "改写后的问句", "domains": ["领域1", "领域2"]}}
